import toInteger from 'date-fns/_lib/toInteger';
import toDate from 'date-fns/toDate/index.d.ts';
import getUTCISOWeek from 'date-fns/_lib/getUTCISOWeek';
import requiredArgs from 'date-fns/_lib/requiredArgs'; // This function will be a part of public API when UTC function will be implemented.
// See issue: https://github.com/date-fns/date-fns/issues/376

export default function setUTCISOWeek(dirtyDate, dirtyISOWeek) {
  requiredArgs(2, arguments);
  var date = toDate(dirtyDate);
  var isoWeek = toInteger(dirtyISOWeek);
  var diff = getUTCISOWeek(date) - isoWeek;
  date.setUTCDate(date.getUTCDate() - diff * 7);
  return date;
}
