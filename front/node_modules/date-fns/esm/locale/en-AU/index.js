import formatDistance from 'date-fns/locale/en-US/_lib/formatDistance';
import formatLong from 'date-fns/locale/en-AU/_lib/formatLong';
import formatRelative from 'date-fns/locale/en-US/_lib/formatRelative';
import localize from 'date-fns/locale/en-US/_lib/localize';
import match from 'date-fns/locale/en-US/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary English locale (Australia).
 * @language English
 * @iso-639-2 eng
 * @author Julien Malige [@JulienMalige]{@link https://github.com/JulienMalige}
 */

var locale = {
  code: 'en-AU',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1
    /* Monday */
    ,
    firstWeekContainsDate: 4
  }
};
export default locale;
