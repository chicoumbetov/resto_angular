import formatDistance from 'date-fns/locale/sr/_lib/formatDistance';
import formatLong from 'date-fns/locale/sr/_lib/formatLong';
import formatRelative from 'date-fns/locale/sr/_lib/formatRelative';
import localize from 'date-fns/locale/sr/_lib/localize';
import match from 'date-fns/locale/sr/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Serbian cyrillic locale.
 * @language Serbian
 * @iso-639-2 srp
 * @author Igor Radivojević [@rogyvoje]{@link https://github.com/rogyvoje}
 */

var locale = {
  code: 'sr',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1
    /* Monday */
    ,
    firstWeekContainsDate: 1
  }
};
export default locale;
