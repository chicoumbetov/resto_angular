import formatDistance from 'date-fns/locale/sv/_lib/formatDistance';
import formatLong from 'date-fns/locale/sv/_lib/formatLong';
import formatRelative from 'date-fns/locale/sv/_lib/formatRelative';
import localize from 'date-fns/locale/sv/_lib/localize';
import match from 'date-fns/locale/sv/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Swedish locale.
 * @language Swedish
 * @iso-639-2 swe
 * @author Johannes Ulén [@ejulen]{@link https://github.com/ejulen}
 * @author Alexander Nanberg [@alexandernanberg]{@link https://github.com/alexandernanberg}
 * @author Henrik Andersson [@limelights]{@link https://github.com/limelights}
 */

var locale = {
  code: 'sv',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1
    /* Monday */
    ,
    firstWeekContainsDate: 4
  }
};
export default locale;
