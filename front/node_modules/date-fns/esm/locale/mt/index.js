import formatDistance from 'date-fns/locale/mt/_lib/formatDistance';
import formatLong from 'date-fns/locale/mt/_lib/formatLong';
import formatRelative from 'date-fns/locale/mt/_lib/formatRelative';
import localize from 'date-fns/locale/mt/_lib/localize';
import match from 'date-fns/locale/mt/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Maltese locale.
 * @language Maltese
 * @iso-639-2 mlt
 * @author Andras Matzon [@amatzon](@link https://github.com/amatzon)
 * @author Bryan Borg [@bryanMt](@link https://github.com/bryanMt)
 */

var locale = {
  code: 'mt',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1
    /* Monday */
    ,
    firstWeekContainsDate: 4
  }
};
export default locale;
