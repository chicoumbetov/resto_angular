import formatDistance from 'date-fns/locale/he/_lib/formatDistance';
import formatLong from 'date-fns/locale/he/_lib/formatLong';
import formatRelative from 'date-fns/locale/he/_lib/formatRelative';
import localize from 'date-fns/locale/he/_lib/localize';
import match from 'date-fns/locale/he/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Hebrew locale.
 * @language Hebrew
 * @iso-639-2 heb
 * @author Nir Lahad [@nirlah]{@link https://github.com/nirlah}
 */

var locale = {
  code: 'he',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 0
    /* Sunday */
    ,
    firstWeekContainsDate: 1
  }
};
export default locale;
