import formatDistance from 'date-fns/locale/et/_lib/formatDistance';
import formatLong from 'date-fns/locale/et/_lib/formatLong';
import formatRelative from 'date-fns/locale/et/_lib/formatRelative';
import localize from 'date-fns/locale/et/_lib/localize';
import match from 'date-fns/locale/et/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Estonian locale.
 * @language Estonian
 * @iso-639-2 est
 * @author Priit Hansen [@HansenPriit]{@link https://github.com/priithansen}
 */

var locale = {
  code: 'et',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1
    /* Monday */
    ,
    firstWeekContainsDate: 4
  }
};
export default locale;
