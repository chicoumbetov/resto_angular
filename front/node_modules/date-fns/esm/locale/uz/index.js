import formatDistance from 'date-fns/locale/uz/_lib/formatDistance';
import formatLong from 'date-fns/locale/uz/_lib/formatLong';
import formatRelative from 'date-fns/locale/uz/_lib/formatRelative';
import localize from 'date-fns/locale/uz/_lib/localize';
import match from 'date-fns/locale/uz/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Uzbek locale.
 * @language Uzbek
 * @iso-639-2 uzb
 * @author Mukhammadali [@mukhammadali]{@link https://github.com/Mukhammadali}
 */

var locale = {
  code: 'uz',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1
    /* Monday */
    ,
    firstWeekContainsDate: 1
  }
};
export default locale;
