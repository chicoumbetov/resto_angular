import formatDistance from 'date-fns/locale/lv/_lib/formatDistance';
import formatLong from 'date-fns/locale/lv/_lib/formatLong';
import formatRelative from 'date-fns/locale/lv/_lib/formatRelative';
import localize from 'date-fns/locale/lv/_lib/localize';
import match from 'date-fns/locale/lv/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Latvian locale (Latvia).
 * @language Latvian
 * @iso-639-2 lav
 * @author Rūdolfs Puķītis [@prudolfs]{@link https://github.com/prudolfs}
 */

var locale = {
  code: 'lv',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1
    /* Monday */
    ,
    firstWeekContainsDate: 4
  }
};
export default locale;
