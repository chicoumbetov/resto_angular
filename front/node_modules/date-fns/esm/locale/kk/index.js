import formatDistance from 'date-fns/locale/kk/_lib/formatDistance';
import formatLong from 'date-fns/locale/kk/_lib/formatLong';
import formatRelative from 'date-fns/locale/kk/_lib/formatRelative';
import localize from 'date-fns/locale/kk/_lib/localize';
import match from 'date-fns/locale/kk/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Kazakh locale.
 * @language Kazakh
 * @iso-639-2 kaz
 * @author Nikita Bayev [@drugoi]{@link https://github.com/drugoi}
 */

var locale = {
  code: 'kk',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1
    /* Monday */
    ,
    firstWeekContainsDate: 1
  }
};
export default locale;
