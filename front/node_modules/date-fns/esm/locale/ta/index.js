import formatDistance from 'date-fns/locale/ta/_lib/formatDistance';
import formatLong from 'date-fns/locale/ta/_lib/formatLong';
import formatRelative from 'date-fns/locale/ta/_lib/formatRelative';
import localize from 'date-fns/locale/ta/_lib/localize';
import match from 'date-fns/locale/ta/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Tamil locale (India).
 * @language Tamil
 * @iso-639-2 tam
 * @author Sibiraj [@sibiraj-s]{@link https://github.com/sibiraj-s}
 */

var locale = {
  code: 'ta',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1
    /* Monday */
    ,
    firstWeekContainsDate: 4
  }
};
export default locale;
