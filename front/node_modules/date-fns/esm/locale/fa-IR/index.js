import formatDistance from 'date-fns/locale/fa-IR/_lib/formatDistance';
import formatLong from 'date-fns/locale/fa-IR/_lib/formatLong';
import formatRelative from 'date-fns/locale/fa-IR/_lib/formatRelative';
import localize from 'date-fns/locale/fa-IR/_lib/localize';
import match from 'date-fns/locale/fa-IR/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Persian/Farsi locale (Iran).
 * @language Persian
 * @iso-639-2 ira
 * @author Morteza Ziyae [@mort3za]{@link https://github.com/mort3za}
 */

var locale = {
  code: 'fa-IR',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 0
    /* Sunday */
    ,
    firstWeekContainsDate: 1
  }
};
export default locale;
