import formatDistance from 'date-fns/locale/az/_lib/formatDistance';
import formatLong from 'date-fns/locale/az/_lib/formatLong';
import formatRelative from 'date-fns/locale/az/_lib/formatRelative';
import localize from 'date-fns/locale/az/_lib/localize';
import match from 'date-fns/locale/az/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Azerbaijani locale.
 * @language Azerbaijani
 * @iso-639-2 aze
 */

var locale = {
  code: 'az',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1,
    firstWeekContainsDate: 1
  }
};
export default locale;
