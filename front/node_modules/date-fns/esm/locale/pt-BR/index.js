import formatDistance from 'date-fns/locale/pt-BR/_lib/formatDistance';
import formatLong from 'date-fns/locale/pt-BR/_lib/formatLong';
import formatRelative from 'date-fns/locale/pt-BR/_lib/formatRelative';
import localize from 'date-fns/locale/pt-BR/_lib/localize';
import match from 'date-fns/locale/pt-BR/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Portuguese locale (Brazil).
 * @language Portuguese
 * @iso-639-2 por
 * @author Lucas Duailibe [@duailibe]{@link https://github.com/duailibe}
 * @author Yago Carballo [@yagocarballo]{@link https://github.com/YagoCarballo}
 */

var locale = {
  code: 'pt-BR',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 0
    /* Sunday */
    ,
    firstWeekContainsDate: 1
  }
};
export default locale;
