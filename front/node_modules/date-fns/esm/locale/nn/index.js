import formatDistance from 'date-fns/locale/nn/_lib/formatDistance';
import formatLong from 'date-fns/locale/nn/_lib/formatLong';
import formatRelative from 'date-fns/locale/nn/_lib/formatRelative';
import localize from 'date-fns/locale/nn/_lib/localize';
import match from 'date-fns/locale/nn/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Norwegian Nynorsk locale.
 * @language Norwegian Nynorsk
 * @iso-639-2 nno
 * @author Mats Byrkjeland [@draperunner]{@link https://github.com/draperunner}
 */

var locale = {
  code: 'nn',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1
    /* Monday */
    ,
    firstWeekContainsDate: 4
  }
};
export default locale;
