import formatDistance from 'date-fns/locale/el/_lib/formatDistance';
import formatLong from 'date-fns/locale/el/_lib/formatLong';
import formatRelative from 'date-fns/locale/el/_lib/formatRelative';
import localize from 'date-fns/locale/el/_lib/localize';
import match from 'date-fns/locale/el/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Greek locale.
 * @language Greek
 * @iso-639-2 ell
 * @author Fanis Katsimpas [@fanixk]{@link https://github.com/fanixk}
 * @author Theodoros Orfanidis [@teoulas]{@link https://github.com/teoulas}
 */

var locale = {
  code: 'el',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1
    /* Monday */
    ,
    firstWeekContainsDate: 4
  }
};
export default locale;
