import formatDistance from 'date-fns/locale/cy/_lib/formatDistance';
import formatLong from 'date-fns/locale/cy/_lib/formatLong';
import formatRelative from 'date-fns/locale/cy/_lib/formatRelative';
import localize from 'date-fns/locale/cy/_lib/localize';
import match from 'date-fns/locale/cy/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Welsh locale.
 * @language Welsh
 * @iso-639-2 cym
 * @author Elwyn Malethan [@elmomalmo]{@link https://github.com/elmomalmo}
 */

var locale = {
  code: 'cy',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 0
    /* Sunday */
    ,
    firstWeekContainsDate: 1
  }
};
export default locale;
