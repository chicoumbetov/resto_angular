import formatDistance from 'date-fns/locale/be/_lib/formatDistance';
import formatLong from 'date-fns/locale/be/_lib/formatLong';
import formatRelative from 'date-fns/locale/be/_lib/formatRelative';
import localize from 'date-fns/locale/be/_lib/localize';
import match from 'date-fns/locale/be/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Belarusian locale.
 * @language Belarusian
 * @iso-639-2 bel
 * @author Kiryl Anokhin [@alyrik]{@link https://github.com/alyrik}
 * @author Martin Wind [@arvigeus]{@link https://github.com/mawi12345}
 */

var locale = {
  code: 'be',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1
    /* Monday */
    ,
    firstWeekContainsDate: 1
  }
};
export default locale;
