import formatDistance from 'date-fns/locale/bg/_lib/formatDistance';
import formatLong from 'date-fns/locale/bg/_lib/formatLong';
import formatRelative from 'date-fns/locale/bg/_lib/formatRelative';
import localize from 'date-fns/locale/bg/_lib/localize';
import match from 'date-fns/locale/bg/_lib/match';
/**
 * @type {Locale}
 * @category Locales
 * @summary Bulgarian locale.
 * @language Bulgarian
 * @iso-639-2 bul
 * @author Nikolay Stoynov [@arvigeus]{@link https://github.com/arvigeus}
 * @author Tsvetan Ovedenski [@fintara]{@link https://github.com/fintara}
 */

var locale = {
  code: 'bg',
  formatDistance: formatDistance,
  formatLong: formatLong,
  formatRelative: formatRelative,
  localize: localize,
  match: match,
  options: {
    weekStartsOn: 1
    /* Monday */
    ,
    firstWeekContainsDate: 1
  }
};
export default locale;
